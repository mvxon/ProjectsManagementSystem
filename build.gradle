plugins {
    id 'java'
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'idea'

    repositories {
        mavenCentral()
    }

    wrapper {
        gradleVersion = '7.5.1'
        distributionType = Wrapper.DistributionType.BIN
    }
}